import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  images: {
    domains: ['images.unsplash.com', "plus.unsplash.com", "media.istockphoto.com", "example.com", 'yourcdn.com'],
  },
};

export default nextConfig;
